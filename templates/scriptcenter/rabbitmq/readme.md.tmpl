# {{.ProjectName}}

脚本中心项目 - RabbitMQ 消息队列集成

## 特性

- ✅ Cobra CLI 框架
- ✅ RabbitMQ 消费者 + 生产者完整实现
- ✅ 支持交换机配置
- ✅ 支持延迟消息
- ✅ MySQL 数据库集成
- ✅ GORM ORM
- ✅ Docker Compose 本地开发环境

## 快速开始

### 使用 Docker Compose 启动依赖

```bash
docker-compose -f deploy/docker-compose.yaml up -d
```

### 构建

```bash
go build -o bin/{{.ProjectName}} cmd/main.go
```

### 运行

```bash
./bin/{{.ProjectName}} start
```

### 使用 CLI 测试

```bash
# 创建文章事件并发送到 RabbitMQ
./bin/{{.ProjectName}} article create --name "Test Article" --data "Test Content"

# 查询文章
./bin/{{.ProjectName}} article get 1
```

## 架构

- **cmd** - CLI 入口和命令定义
- **internal/handler** - CLI 处理器
- **internal/service** - 业务服务层
- **internal/model** - 数据模型
- **internal/repository** - 数据访问层
- **internal/mq** - RabbitMQ 客户端封装
- **pkg/config** - 配置加载
- **pkg/database** - 数据库连接初始化

## 许可证

Apache 2.0
