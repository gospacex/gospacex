package rpcclient

import (
	"google.golang.org/grpc"
	"google.golang.org/grpc/credentials/insecure"
	"{{.ModuleName}}/common/kitexGen/{{.TableName}}"
)

type {{.UpperName}}Client struct {
	conn *grpc.ClientConn
	client {{.TableName}}.{{.UpperName}}ServiceClient
}

func New{{.UpperName}}Client(addr string) (*{{.UpperName}}Client, error) {
	conn, err := grpc.Dial(addr, grpc.WithTransportCredentials(insecure.NewCredentials()))
	if err != nil {
		return nil, err
	}
	return &{{.UpperName}}Client{conn: conn, client: {{.TableName}}.New{{.UpperName}}ServiceClient(conn)}, nil
}
