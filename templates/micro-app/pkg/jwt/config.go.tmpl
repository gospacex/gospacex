package jwt

import (
	"time"

	"github.com/golang-jwt/jwt/v5"
)

// DefaultConfig 返回默认配置
func DefaultConfig() *Config {
	return &Config{
		SigningMethod: jwt.SigningMethodHS256,
		Issuer:         "gpx-app",
		Expiration:     3600,       // 1 hour
		RefreshWindow:  604800,     // 7 days
	}
}